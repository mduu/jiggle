import * as React from 'react';
import './Albums.css';

const Albums = ({}) => (

  <section className="Albums">
    <h1>Albums</h1>
    <p>Under constuction...</p>
  </section>
);

export default Albums;